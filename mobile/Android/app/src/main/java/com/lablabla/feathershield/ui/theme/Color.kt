package com.lablabla.feathershield.ui.theme

import androidx.compose.ui.graphics.Color

// Primary Blues from the logo
val PrimaryBlueDark = Color(0xFF003366) // Darker shield background
val PrimaryBlueLight = Color(0xFF0055AA) // Lighter shield background / accent
val FeatherBlue = Color(0xFF00BFFF)    // Feather color - a vibrant blue

// Grays for backgrounds, surfaces, and text
val Grey800 = Color(0xFF212121)       // Dark background for dark theme
val Grey900 = Color(0xFF121212)       // Even darker for dark theme
val Grey200 = Color(0xFFEEEEEE)       // Light surface for light theme
val Grey50 = Color(0xFFFAFAFA)        // Lighter background for light theme
val SilverAccent = Color(0xFFB0BEC5)  // For borders/accents (like the shield outline)

// Standard text colors (can be adjusted based on background)
val TextDark = Color(0xFF212121)
val TextLight = Color(0xFFFFFFFF)

// Other common colors
val ErrorRed = Color(0xFFB00020)
val SuccessGreen = Color(0xFF388E3C)